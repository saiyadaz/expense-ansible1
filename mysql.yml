- name: Mysql setup
  hosts: all
  become: yes
  tasks:
    - name: Install Mysql server
      ansible.builtin.dnf:
        name: mysql-server
        state: installed

    - name: Start mysql service
      ansible.builtin.systemd_service:
        name: mysqld
        state: started
        enabled: yes


    - name: Check the DB connection with our password
      community.mysql.mysql_info:
        login_user: root
        login_password: "{{mysql_root_password}}"
        login_host: mysql-dev.devopssz14.online
        filter: version
      register: mysql_connection_output
      ignore_errors: yes

    - name: Setup MySQL Password
      ansible.builtin.shell: mysql_secure_installation --set-root-pass {{mysql_root_password}}
      when: mysql_connection_output.failed is true
    - name: Setup Mysql Password
      ansible.builtin.shell: mysql_secure_installation --set-root-pass ExpenseApp@1

      # it will fail
      # in project we have to use amazon services for database-rds